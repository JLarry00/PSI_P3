# # render.yaml – configuración de despliegue en render.com
# #
# # URL resultante debe seguir el formato:
# #   https://3-songproject-<PAREJA>-<GRUPO>-<AÑO>-<VERSION>.onrender.com
# #
# # Pasos antes de hacer push:
# #   1. Cambia el campo "name" con tus datos reales (pareja, grupo, año, versión).
# #   2. Crea la base de datos en neon.tech y copia la connection string.
# #   3. En el dashboard de render.com añade la variable de entorno DATABASE_URL
# #      con la connection string de neon.tech (marca la opción "Secret").
# #   4. Haz push y render.com ejecutará el buildCommand automáticamente.

# services:
#   - type: web
#     name: 3-songproject-99--2026-v1   # <-- cambia PAREJA, GRUPO y v1
#     env: python
#     region: frankfurt
#     rootDir: backend-p3
#     buildCommand: >
#       pip install -r requirements.txt &&
#       python manage.py collectstatic --no-input &&
#       python manage.py migrate &&
#       python manage.py populate
#     startCommand: gunicorn songproject.wsgi:application --bind 0.0.0.0:$PORT
#     envVars:
#       - key: DJANGO_SECRET_KEY
#         generateValue: true          # render.com genera una clave aleatoria
#       - key: DEBUG
#         value: "False"
#       - key: ALLOWED_HOSTS
#         value: ".onrender.com"
#       - key: DATABASE_URL
#         sync: false                  # se configura manualmente en el dashboard con la URI de neon.tech
